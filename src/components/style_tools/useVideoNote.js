import {useEffect, useState} from "react";

export const useVideoNote = () => {
    const [show, setShow] = useState(null);
    const [video, setVideo] = useState({});

    useEffect(()=>{
            function handleStatusChange(status){
                status
            }
        }
    );


}